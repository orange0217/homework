apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  labels:
    app.kubernetes.io/name: jenkins
  name: jenkins
  namespace: jenkins
spec:
  ingressClassName: nginx
  rules:
  - host: jenkins.starmooc.net
    http:
      paths: 
      - backend:
          service:
            name: jenkins
            port:
              number: 8080
        pathType: ImplementationSpecific
        path: /
  tls:
  - hosts:
    - jenkins.starmooc.net
    secretName: jenkins-crt-secret 


